# Machine-Learning
Machine learning is a branch of artificial intelligence that lets computers learn from data and improve from experience without being explicitly programmed for every single task. Instead of writing rigid step-by-step rules, developers feed large amounts of data into algorithms so the system can find patterns on its own.
